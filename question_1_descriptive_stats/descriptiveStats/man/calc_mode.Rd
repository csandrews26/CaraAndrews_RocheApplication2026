% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_mode.R
\name{calc_mode}
\alias{calc_mode}
\title{Calculate mode}
\usage{
calc_mode(x, na.rm = FALSE)
}
\arguments{
\item{x}{A numeric vector. Must not be empty.}

\item{na.rm}{Logical. Should missing values be removed before calculation?
Defaults to \code{FALSE}.}
}
\value{
The numeric value(s) representing the mode of \code{x}. Returns all tied
values when more than one value shares the highest frequency. Returns \code{NA}
if every value appears exactly once (no mode exists).
}
\description{
Returns the modal value(s) of a numerical vector, with control over
how \code{NA} values are handled.
}
\examples{
calc_mode(c(1, 2, 2, 3, 4, 5, 5, 5, 6, 10))  # 5
calc_mode(c(1, 1, 2, 2, 3))                  # 1 2  (tie)
calc_mode(c(1, 2, 3, 4))                     # NA  (no mode)
calc_mode(c(1, 2, NA, NA), na.rm = FALSE)    # NA  (NA is modal)
calc_mode(c(1, 2, NA, NA), na.rm = TRUE)     # NA  (no mode after removal)
}
\author{
Cara Andrews
}
